Django==5.2.6
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.2

# IBM Db2. `ibm_db` o'z ichiga CLI drayverini (clidriver) olib keladi -
# tizimga alohida drayver o'rnatish shart emas.
ibm_db==3.3.0
ibm_db_django==1.6.0.0
# ibm_db_django 1.6.0.0 `schemaEditor.py` da jinja2 ni import qiladi, lekin
# uni bogliqlik sifatida elon qilmagan (paketlash xatosi) - ozimiz qoshamiz.
jinja2==3.1.6
python-dotenv==1.1.1
# WSGI server (gunicorn) YO'Q: WebSocket kerak, shuning uchun ASGI - daphne
# (`Dockerfile` dagi CMD). `config/wsgi.py` joyida qoladi, lekin ishlatilmaydi.
whitenoise==6.9.0
Pillow==11.3.0

# real-time (WebSocket)
channels==4.2.0
channels-redis==4.2.1
daphne==4.1.2

# Produksiyada Daphne o'rniga: ko'p ishchi (worker) jarayoni bilan
# ishlaydi, ya'ni 1000+ ulanishda bitta jarayon bottleneck bo'lmaydi.
# `[standard]` - uvloop va httptools qo'shiladi (Linux da ~2x tezroq).
# DEBUG rejimda `runserver` qoladi, shuning uchun dev muhitga ta'siri yo'q.
uvicorn[standard]==0.34.0
